# 开发文档

## 提交策略

仓库继续默认忽略一般 Markdown 文件，避免临时笔记误入提交；`开发文档/**/*.md` 已在 `.gitignore` 中白名单，新增或调整的开发文档会出现在普通 `git status` 中，可直接 `git add 开发文档/...` 提交。

若确需提交 `开发文档` 之外仍被忽略的 Markdown 文件，请显式使用 `git add -f <path>.md`，并在提交说明中注明原因。
